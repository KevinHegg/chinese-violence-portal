# Deprecated: lynchings.json

`lynchings.json` is deprecated. All publication chart exports now use the live Google Sheet (Main tab) as the data source.

The export script `scripts/export-publication-charts.mjs` fetches directly from the Chinese lynching master dataset.
